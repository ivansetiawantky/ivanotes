# Use and install locally `inkscape + $\LaTeX$` to draw math illustration  (result is `.svg` file)

To draw math illustration, it is good to use `inkscape`.

`inkscape` inside Docker container seems slow, so install directly to host. And, to enable writing $\LaTeX$ formula to the svg, install `$\LaTeX$` locally also.